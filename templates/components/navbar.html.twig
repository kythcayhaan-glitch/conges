<nav id="sidebar" class="bg-primary text-white">
    <a class="sidebar-brand text-white d-flex align-items-center gap-2" href="{{ path('app_dashboard') }}">
        <i class="bi bi-calendar-check fs-5"></i> Congés
    </a>

    <ul class="nav flex-column mt-2 flex-grow-1">
        <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_dashboard') }}">
                <i class="bi bi-house"></i> Tableau de bord
            </a>
        </li>
        <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_leave_calendar') }}">
                <i class="bi bi-calendar3-week"></i> Calendrier
            </a>
        </li>

        <li class="nav-section-label">Absences</li>

        <li class="nav-item">
            <a class="nav-link text-white dropdown-toggle" data-bs-toggle="collapse" href="#menuConge" role="button">
                <i class="bi bi-calendar3"></i> Congés
            </a>
            <div class="collapse" id="menuConge">
                <ul class="nav flex-column">
                    <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'CONGE'}) }}">
                        <i class="bi bi-list-ul"></i> Liste des demandes
                    </a></li>
                    <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'CONGE'}) }}">
                        <i class="bi bi-plus-circle"></i> Nouvelle demande
                    </a></li>
                </ul>
            </div>
        </li>

        <li class="nav-item">
            <a class="nav-link text-white dropdown-toggle" data-bs-toggle="collapse" href="#menuRtt" role="button">
                <i class="bi bi-clock"></i> RTT
            </a>
            <div class="collapse" id="menuRtt">
                <ul class="nav flex-column">
                    <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'RTT'}) }}">
                        <i class="bi bi-list-ul"></i> Liste des demandes
                    </a></li>
                    <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'RTT'}) }}">
                        <i class="bi bi-plus-circle"></i> Nouvelle demande
                    </a></li>
                </ul>
            </div>
        </li>

        <li class="nav-item">
            <a class="nav-link text-white dropdown-toggle" data-bs-toggle="collapse" href="#menuHeureSup" role="button">
                <i class="bi bi-hourglass-split"></i> Heures sup.
            </a>
            <div class="collapse" id="menuHeureSup">
                <ul class="nav flex-column">
                    <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'HEURE_SUP'}) }}">
                        <i class="bi bi-list-ul"></i> Liste des demandes
                    </a></li>
                    <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'HEURE_SUP'}) }}">
                        <i class="bi bi-plus-circle"></i> Nouvelle demande
                    </a></li>
                </ul>
            </div>
        </li>

        {% if is_granted('ROLE_RH') %}
        <li class="nav-section-label">Gestion</li>
        <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_agents_list') }}">
                <i class="bi bi-people"></i> Agents
            </a>
        </li>
        {% endif %}

        {% if is_granted('ROLE_ADMIN') %}
        <li class="nav-item">
            <a class="nav-link text-white" href="{{ path('app_admin_users') }}">
                <i class="bi bi-shield-lock"></i> Administration
            </a>
        </li>
        {% endif %}
    </ul>

    <div class="sidebar-footer">
        <div class="d-flex align-items-center justify-content-between px-1 mb-1">
            <span class="text-white-50 small">
                <i class="bi bi-person-circle me-1"></i>
                {{ app.user.prenom }} {{ app.user.nom }}
                {% if app.user.serviceNumbers is not empty %}
                    {% for sn in app.user.serviceNumbers %}<span class="ms-1">– Svc {{ sn }}</span>{% endfor %}
                {% endif %}
            </span>
            <button id="theme-toggle" class="btn btn-link nav-link text-white px-1" title="Thème"></button>
        </div>
        <ul class="nav flex-column">
            {% if not is_granted('ROLE_RH') %}
            <li><a class="dropdown-item" href="{{ path('app_agents_profile') }}">
                <i class="bi bi-person"></i> Mon profil
            </a></li>
            {% endif %}
            <li><a class="dropdown-item" href="{{ path('app_account_password') }}">
                <i class="bi bi-lock"></i> Changer de mot de passe
            </a></li>
            <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                <i class="bi bi-box-arrow-right"></i> Déconnexion
            </a></li>
        </ul>
    </div>
</nav>
