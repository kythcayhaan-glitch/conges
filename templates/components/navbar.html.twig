<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand fw-bold" href="{{ path('app_dashboard') }}">
            <i class="bi bi-calendar-check me-1"></i> Congés
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navMenu">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_dashboard') }}">
                        <i class="bi bi-house me-1"></i>Tableau de bord
                    </a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-calendar3 me-1"></i>Congés
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'CONGE'}) }}">
                            <i class="bi bi-list-ul me-1"></i>Liste des demandes
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'CONGE'}) }}">
                            <i class="bi bi-plus-circle me-1"></i>Nouvelle demande
                        </a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-clock me-1"></i>RTT
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'RTT'}) }}">
                            <i class="bi bi-list-ul me-1"></i>Liste des demandes
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'RTT'}) }}">
                            <i class="bi bi-plus-circle me-1"></i>Nouvelle demande
                        </a></li>
                    </ul>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-hourglass-split me-1"></i>Heures suppémentaires.
                    </a>
                    <ul class="dropdown-menu">
                        <li><a class="dropdown-item" href="{{ path('app_leave_list', {type: 'HEURE_SUP'}) }}">
                            <i class="bi bi-list-ul me-1"></i>Liste des demandes
                        </a></li>
                        <li><a class="dropdown-item" href="{{ path('app_leave_new', {type: 'HEURE_SUP'}) }}">
                            <i class="bi bi-plus-circle me-1"></i>Nouvelle demande
                        </a></li>
                    </ul>
                </li>
                {% if is_granted('ROLE_RH') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_agents_list') }}">
                        <i class="bi bi-people me-1"></i>Agents
                    </a>
                </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_admin_users') }}">
                        <i class="bi bi-shield-lock me-1"></i>Administration
                    </a>
                </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-1"></i>{{ app.user.prenom }} {{ app.user.nom }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        {% if not is_granted('ROLE_RH') %}
                        <li>
                            <a class="dropdown-item" href="{{ path('app_agents_profile') }}">
                                <i class="bi bi-person me-1"></i>Mon profil
                            </a>
                        </li>
                        {% endif %}
                        <li>
                            <a class="dropdown-item" href="{{ path('app_account_password') }}">
                                <i class="bi bi-lock me-1"></i>Changer de mot de passe
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right me-1"></i>Déconnexion
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
